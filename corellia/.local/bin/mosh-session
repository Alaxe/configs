#!/usr/bin/perl -w
use strict;

my $shell = exists $ENV{ 'SHELL' } ? $ENV{ 'SHELL' } : (getpwuid( $> ))[ 8 ];

$ENV{'TERM'} = 'xterm-256color';  # force 256-color TERM
$ENV{'COLORTERM'} = 'truecolor';

system $shell "-$shell";

system 'kdestroy';
